# ==============================================================================
# CKB Spellchecker - Environment Configuration Sample
# ==============================================================================
# This is a sample configuration file. It provides templates for both local
# development and production (Toolforge) environments.
#
# --- INSTRUCTIONS ---
# 1. Copy this file to a new file named `.env`
# 2. Fill in the values below based on whether you are setting up a local or
#    production environment.
# 3. Ensure the real `.env` file is listed in your .gitignore and NEVER committed.
# 4. For production, remember to set `.env` file permissions with `chmod 600 .env` after creating it.
# ==============================================================================

# --- Environment Mode (Required for all setups) ---
# Sets the application's operating mode.
# Use "development" for local testing.
# Use "production" for deploying on Toolforge.
FLASK_ENV="development"


# --- Database Name (Required for all setups) ---
# The name of the database the application will connect to.
#
# Example for local development:
# DB_NAME="ckb_spellchecker_db"
#
# Example for production on Toolforge:
# DB_NAME="s12345__your_tool_db_name"
# See: https://wikitech.wikimedia.org/wiki/Help:Toolforge/ToolsDB
DB_NAME=""


# --- Local Development Database Credentials ---
# These are ONLY needed when FLASK_ENV is "development".
# On Toolforge, credentials are read automatically from ~/replica.my.cnf.
DB_HOST="127.0.0.1"
DB_USER="root"
DB_PASSWORD=""


# --- Flask Security Configuration (Required for all setups) ---
# A strong, unique secret key. This MUST be set for both environments.
# To generate a new key, run this command in your terminal:
# python -c 'import secrets; print(secrets.token_urlsafe())'
FLASK_SECRET_KEY=""