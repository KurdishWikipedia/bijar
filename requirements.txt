# ==============================================================================
# requirements.txt - Development and Production Dependencies
# ==============================================================================
# These are the essential packages required to run the application on Toolforge.
# To install, run: pip install -r requirements.txt
# ------------------------------------------------------------------------------

# -- Web Framework & Configuration --
flask
flask-cors              # Enables cross-origin requests between a browser and a Flask backend on different domains.
python-dotenv           # Reads environment variables from the .env file

# -- Database --
PyMySQL                 # Pure-Python MySQL driver for database connections

# -- Core Logic & Performance --
Levenshtein             # Fast C implementation for string distance calculation
psutil                  # Used for monitoring system memory usage on startup